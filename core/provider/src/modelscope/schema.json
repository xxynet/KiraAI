{
  "provider_config": {
    "api_key": {
      "name": "API 密钥",
      "type": "sensitive",
      "default": null,
      "hint": "ModelScope API 密钥"
    }
  },
  "model_config": {
    "llm": {
      "timeout": {
        "name": "超时",
        "type": "integer",
        "default": 120,
        "min": 1,
        "hint": "超时秒数"
      },
      "temperature": {
        "name": "温度",
        "type": "float",
        "default": null,
        "min": 0,
        "max": 2,
        "hint": "模型温度"
      }
    },
    "image": {
      "timeout": {
        "name": "超时时间",
        "type": "integer",
        "default": 30,
        "min": 1,
        "hint": "生图超时时间（秒）"
      }
    },
    "embedding": {
      "timeout": {
        "name": "超时",
        "type": "integer",
        "default": 60,
        "min": 1,
        "hint": "请求超时时间（秒）"
      },
      "slow_request_threshold": {
        "name": "慢请求阈值",
        "type": "float",
        "default": null,
        "hint": "超过此阈值（秒）的请求将被记录为慢请求，null 表示禁用慢请求记录"
      }
    }
  }
}