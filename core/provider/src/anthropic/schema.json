{
  "provider_config": {
    "base_url": {
      "name": "Base URL",
      "type": "string",
      "default": "https://api.anthropic.com",
      "hint": "Base URL of an Anthropic-compatible API; keep provider-specific path prefixes (for example, DeepSeek uses https://api.deepseek.com/anthropic)",
      "locales": {
        "zh": { "name": "Base URL", "hint": "兼容 Anthropic 格式 API 的 Base URL；请保留平台要求的路径前缀（例如 DeepSeek 使用 https://api.deepseek.com/anthropic）" }
      }
    },
    "api_key": {
      "name": "API Key",
      "type": "sensitive",
      "default": null,
      "hint": "API key sent in the x-api-key header",
      "locales": {
        "zh": { "name": "API 密钥", "hint": "通过 x-api-key 请求头发送的 API 密钥" }
      }
    },
    "anthropic_version": {
      "name": "Anthropic API Version",
      "type": "string",
      "default": "2023-06-01",
      "hint": "Value of the anthropic-version request header",
      "locales": {
        "zh": { "name": "Anthropic API 版本", "hint": "anthropic-version 请求头的值" }
      }
    },
    "section_advanced": {
      "type": "section",
      "name": "Advanced Settings",
      "hint": "Advanced provider configuration options",
      "collapsed": true,
      "locales": {
        "zh": { "name": "高级设置", "hint": "高级提供商配置选项" }
      },
      "fields": {
        "headers": {
          "name": "Custom Headers",
          "type": "json",
          "default": {},
          "hint": "Additional request headers; matching keys override defaults",
          "locales": {
            "zh": { "name": "自定义请求头", "hint": "附加请求头；同名字段会覆盖默认值" }
          }
        }
      }
    }
  },
  "model_config": {
    "llm": {
      "max_tokens": {
        "name": "Max Output Tokens",
        "type": "integer",
        "default": 4096,
        "min": 1,
        "hint": "Maximum number of tokens to generate",
        "locales": {
          "zh": { "name": "最大输出 Token 数", "hint": "单次响应最多生成的 Token 数" }
        }
      },
      "timeout": {
        "name": "Timeout",
        "type": "integer",
        "default": 120,
        "min": 1,
        "hint": "Request timeout in seconds",
        "locales": {
          "zh": { "name": "超时", "hint": "请求超时秒数" }
        }
      },
      "section_advanced": {
        "type": "section",
        "name": "Advanced Settings",
        "hint": "Advanced model configuration options",
        "collapsed": true,
        "locales": {
          "zh": { "name": "高级设置", "hint": "高级模型配置选项" }
        },
        "fields": {
          "temperature": {
            "name": "Temperature",
            "type": "float",
            "default": null,
            "min": 0,
            "max": 1,
            "hint": "Model temperature",
            "locales": {
              "zh": { "name": "温度", "hint": "模型温度" }
            }
          },
          "extra_body": {
            "name": "Extra Body",
            "type": "json",
            "default": {},
            "hint": "Additional fields merged into the request body",
            "locales": {
              "zh": { "name": "自定义请求体", "hint": "合并到请求体中的额外字段" }
            }
          }
        }
      }
    }
  }
}
