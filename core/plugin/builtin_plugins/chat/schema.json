{
  "waking_words": {
    "name": "Waking words",
    "type": "list",
    "default": [],
    "hint": "Treat message as mentioned if any wake word appears"
  },
  "receive_unmentioned": {
    "name": "Receive Unmentioned Messages",
    "type": "switch",
    "default": false,
    "hint": "Pass unmentioned messages as context"
  },
  "max_unmentioned_messages": {
    "name": "Max Unmentioned Messages",
    "type": "integer",
    "default": 5,
    "hint": "Max unmentioned messages to include in context buffer"
  },
  "group_chat_prompt": {
    "name": "Group Chat Prompt",
    "type": "markdown",
    "default": "",
    "hint": "Extra prompt to guide AI behavior in group chats"
  },
  "group_proactive_chat": {
    "name": "Group Proactive Chat",
    "type": "switch",
    "default": false,
    "hint": "Allow AI to send messages proactively in group chats, be sure to have receive_unmentioned enabled"
  },
  "group_proactive_chat_probability": {
    "name": "Group Proactive Chat Probability",
    "type": "float",
    "default": 0.1,
    "hint": "Probability of triggering a proactive message (default 0.1)"
  }
}