{
  "bot_token": {
    "name": "Bot Token",
    "type": "string",
    "default": "",
    "hint": "Discord Bot Token (from Discord Developer Portal)",
    "locales": {
      "zh": { "name": "Bot Token", "hint": "Discord Bot Token（从 Discord 开发者门户获取）" }
    }
  },
  "proxy": {
    "name": "Proxy",
    "type": "string",
    "default": "",
    "hint": "HTTP proxy URL for connecting to Discord, e.g. http://127.0.0.1:7890",
    "locales": {
      "zh": { "name": "代理", "hint": "连接 Discord 用的 HTTP 代理地址，如 http://127.0.0.1:7890" }
    }
  },
  "intents": {
    "name": "Gateway Intents",
    "type": "list",
    "default": ["guilds", "guild_messages", "message_content", "direct_messages"],
    "hint": "Discord Gateway Intents to enable",
    "locales": {
      "zh": { "name": "网关意图", "hint": "需要启用的 Discord Gateway Intents" }
    }
  },
  "permission_mode": {
    "name": "Permission Mode",
    "type": "string",
    "options": ["allow_list", "deny_list"],
    "default": "allow_list",
    "hint": "Allow list or deny list",
    "locales": {
      "zh": { "name": "权限模式", "hint": "白名单或黑名单" }
    }
  },
  "group_allow_list": {
    "name": "Channel Allow List",
    "type": "list",
    "default": [],
    "hint": "Channel IDs allow list (when permission mode is allow_list)",
    "locales": {
      "zh": { "name": "频道白名单", "hint": "频道 ID 白名单（权限模式为白名单时生效）" }
    }
  },
  "user_allow_list": {
    "name": "User Allow List",
    "type": "list",
    "default": [],
    "hint": "User IDs allow list (when permission mode is allow_list)",
    "locales": {
      "zh": { "name": "用户白名单", "hint": "用户 ID 白名单（权限模式为白名单时生效）" }
    }
  },
  "group_deny_list": {
    "name": "Channel Deny List",
    "type": "list",
    "default": [],
    "hint": "Channel IDs deny list (when permission mode is deny_list)",
    "locales": {
      "zh": { "name": "频道黑名单", "hint": "频道 ID 黑名单（权限模式为黑名单时生效）" }
    }
  },
  "user_deny_list": {
    "name": "User Deny List",
    "type": "list",
    "default": [],
    "hint": "User IDs deny list (when permission mode is deny_list)",
    "locales": {
      "zh": { "name": "用户黑名单", "hint": "用户 ID 黑名单（权限模式为黑名单时生效）" }
    }
  },
  "debug_mode": {
    "name": "Debug Mode",
    "type": "switch",
    "default": false,
    "hint": "Print received raw messages at DEBUG level",
    "locales": {
      "zh": { "name": "调试模式", "hint": "开启后，会以 DEBUG 等级打印收到的原始消息" }
    }
  },
  "debug_mode_list": {
    "name": "Debug Mode Channels",
    "type": "list",
    "default": [],
    "hint": "Channels to enable debug mode for, e.g.:\ngm:<channel_id>\ndm:<user_id>",
    "locales": {
      "zh": { "name": "调试模式频道", "hint": "要开启调试模式的频道，例如：\ngm:<channel_id>\ndm:<user_id>" }
    }
  },
  "slash_guild_ids": {
    "name": "Slash Guild IDs",
    "type": "list",
    "default": [],
    "hint": "Guild IDs to scope slash commands to (global commands if empty)",
    "locales": {
      "zh": { "name": "斜杠命令服务器 ID", "hint": "限定斜杠命令的服务器 ID（留空则为全局命令）" }
    }
  },
  "auto_sync_commands": {
    "name": "Auto Sync Commands",
    "type": "switch",
    "default": true,
    "hint": "Automatically sync slash commands with Discord on startup",
    "locales": {
      "zh": { "name": "自动同步命令", "hint": "启动时自动与 Discord 同步斜杠命令" }
    }
  }
}
